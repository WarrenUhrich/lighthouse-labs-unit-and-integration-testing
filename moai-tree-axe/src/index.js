import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import MoaiTreeAxe from './components/moai-tree-axe';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <MoaiTreeAxe />
  </React.StrictMode>
);
